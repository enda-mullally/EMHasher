# Copilot Instructions

## Project Guidelines
- User prefers reusing existing project icons/graphics for UI consistency, specifically using the existing hash icon from FileHashControl for the Hashes navigation tab.