<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="EM.Hasher.Views.SettingsShell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkitConverters="using:CommunityToolkit.WinUI.Converters"
    NavigationCacheMode="Required"
    mc:Ignorable="d">
    <Page.Resources>
        <toolkitConverters:BoolToVisibilityConverter x:Key="BoolToVisibility" />

        <!--  Style (inc. the correct spacing) of a section header  -->
        <Style
            x:Key="SettingsSectionHeaderTextBlockStyle"
            BasedOn="{StaticResource BodyStrongTextBlockStyle}"
            TargetType="TextBlock">
            <Style.Setters>
                <Setter Property="Margin" Value="1,8,0,4" />
            </Style.Setters>
        </Style>

        <!--  Style for the breadcrumb separator  -->
        <Style
            x:Key="BreadcrumbSeparatorStyle"
            BasedOn="{StaticResource BodyStrongTextBlockStyle}"
            TargetType="TextBlock">
            <Style.Setters>
                <Setter Property="Margin" Value="4,8,4,4" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
            </Style.Setters>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Settings Breadcrumb Header  -->
        <StackPanel
            Grid.Row="0"
            Margin="10,0,0,0"
            Orientation="Horizontal">
            <TextBlock
                Style="{StaticResource SettingsSectionHeaderTextBlockStyle}"
                Text="Settings"
                Tapped="OnSettingsBreadcrumbTapped" />
            <TextBlock
                Style="{StaticResource BreadcrumbSeparatorStyle}"
                Text="  &gt;  "
                Visibility="{x:Bind UiStateViewModel.IsSettingsSubPageVisible, Converter={StaticResource BoolToVisibility}, Mode=OneWay}" />
            <TextBlock
                Style="{StaticResource SettingsSectionHeaderTextBlockStyle}"
                Text="{x:Bind UiStateViewModel.SettingsSubPageTitle, Mode=OneWay}"
                Visibility="{x:Bind UiStateViewModel.IsSettingsSubPageVisible, Converter={StaticResource BoolToVisibility}, Mode=OneWay}" />
        </StackPanel>

        <!--  Settings Content Frame  -->
        <Frame
            x:Name="settingsFrame"
            Grid.Row="1"
            Margin="0,0,0,0"
            Padding="10"
            Background="Transparent"
            Navigated="SettingsFrame_Navigated" />
    </Grid>
</Page>
