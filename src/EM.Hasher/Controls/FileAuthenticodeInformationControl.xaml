<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="EM.Hasher.Controls.FileAuthenticodeInformationControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:EM.Hasher.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkitConverters="using:CommunityToolkit.WinUI.Converters"
    mc:Ignorable="d">

    <UserControl.Resources>
        <toolkitConverters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Certificate Icon  -->
        <FontIcon
            Grid.Column="0"
            Margin="3,0,0,0"
            HorizontalAlignment="Left"
            Glyph="&#xEB95;" />

        <!--
            Loading grid
        -->

        <Grid
            Grid.Column="1"
            Margin="0,0,0,0"
            Visibility="{x:Bind IsLoading, Converter={StaticResource BoolToVisibility}, Mode=OneWay}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  Row 0: Loading  -->
            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                Margin="0,0,20,3"
                Text="{x:Bind LoadingText, Mode=OneWay}"
                TextWrapping="Wrap" />

            <!--  Row 1: ProgressBar  -->
            <ProgressBar
                Grid.Row="1"
                Grid.Column="0"
                Width="200"
                Margin="0,10,0,0"
                HorizontalAlignment="Left"
                IsIndeterminate="True"
                Maximum="100"
                Minimum="0"
                ShowError="False"
                ShowPaused="False" />
        </Grid>

        <!--
            Details grid with aligned rows
        -->

        <Grid
            Grid.Column="1"
            Grid.ColumnSpan="2"
            Margin="0,0,0,0"
            Visibility="{x:Bind IsLoading, Converter={StaticResource BoolToVisibility}, ConverterParameter=True, Mode=OneWay}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  Row 0: Signer  -->
            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                Margin="0,0,20,3"
                Foreground="#8bb7f0"
                Text="Signer"
                TextWrapping="Wrap" />
            <TextBlock
                Grid.Row="0"
                Grid.Column="1"
                Margin="0,0,0,3"
                IsTextSelectionEnabled="True"
                Text="{x:Bind Signer, Mode=OneWay}"
                TextWrapping="Wrap" />

            <!--  Row 1: Issuer  -->
            <TextBlock
                Grid.Row="1"
                Grid.Column="0"
                Margin="0,0,20,3"
                Foreground="#8bb7f0"
                Text="Issuer" />
            <TextBlock
                Grid.Row="1"
                Grid.Column="1"
                Margin="0,0,0,3"
                IsTextSelectionEnabled="True"
                Text="{x:Bind Issuer, Mode=OneWay}" />

            <!--  Row 2: Signing Time  -->
            <TextBlock
                Grid.Row="2"
                Grid.Column="0"
                Margin="0,0,20,3"
                Foreground="#8bb7f0"
                Text="Signed"
                Visibility="{x:Bind IsTimeStamped, Converter={StaticResource BoolToVisibility}, Mode=OneWay}" />
            <TextBlock
                Grid.Row="2"
                Grid.Column="1"
                Margin="0,0,0,3"
                IsTextSelectionEnabled="True"
                Text="{x:Bind SigningTime, Mode=OneWay}"
                Visibility="{x:Bind IsTimeStamped, Converter={StaticResource BoolToVisibility}, Mode=OneWay}" />
        </Grid>
    </Grid>
</UserControl>
